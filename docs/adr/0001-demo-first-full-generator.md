# Demo-first delivery, full generator from day one

Hackathon time is the constraint. v1 is a working 3-minute demo plus a real eval harness, not a blocked dashboard waiting on 5000-row numbers.

The generator must be able to emit the full 4000 development + 1000 hold-out (5000 total) from Phase 2. First eval runs may be smaller. Same split rules, same hidden ground truth. Do not tune on hold-out.

**Status:** accepted
